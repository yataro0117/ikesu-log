## いけすログ 進捗メモ
=== セッション 2026-03-04 ===

[完了した作業]
1. 状態追跡ファイル作成 (tests.json, progress.txt, init.sh, MEMORY.md)
2. Docker Compose (db + backend + frontend)
3. FastAPI バックエンド:
   - models: User, Farm, Site, Cage, Lot, CageLot, Event, Attachment, FeedRateRule, Alert
   - Alembic マイグレーション (0001_initial)
   - Seed データ (生簀10, 魚群4, ユーザー2, FeedRateRule 7件)
   - Routers: auth, cages, events, lots, kpi
   - Services: kpi.py (推定尾数/バイオマス/FCR/SGR/死亡率/出荷見込み)
4. Next.js フロントエンド:
   - App Router (main group): /today, /cages, /input, /analytics, /harvest
   - BottomSheet QuickInputSheet (給餌/死亡/計測/投薬/環境/メモ + 前回値コピー)
   - KpiCard, EventList, CageMap (グリッドマップ), TabBar
   - QR リダイレクト (/qr/[token])
   - オフライン同期 (IndexedDB + /sync/push)
   - PWA manifest.json
5. 全テスト通過 (16件):
   - test_api.py: auth/qr/events all pass
   - test_kpi.py: 計算ロジック all pass

[確認済みAPI]
- GET /health → ok
- POST /auth/login → JWT取得
- GET /auth/me → ユーザー情報
- GET /cages → 10件
- GET /qr/{token} → cage_id返却
- POST /events → FEEDING/MORTALITY/SAMPLING 作成
- GET /kpi/cage/1 → KPI計算値
- GET /kpi/today/todos → 9件 (今日給餌未入力)

[サービス起動状態]
- db: PostgreSQL 16 (port 5432)
- backend: FastAPI (port 8000) ✅
- frontend: Next.js 14 (port 3000) ✅

[既知の注意事項]
- .local TLD は pydantic EmailStr で弾かれるため str を使用
- bcrypt: passlib を使わず直接 bcrypt ライブラリを使用
- テスト: StaticPool で SQLite in-memory 共有

[次のセッションでやること]
- Vercel/Railway デプロイ設定
- CORS設定の本番環境向け調整
- フロントエンドのエラーハンドリング強化
- 魚群(Lot)受入UI
